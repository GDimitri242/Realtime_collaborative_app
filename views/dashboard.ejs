<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accueil - CIRAS MEET</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <nav class="navbar fixed-top" style="background-color: var(--bg-light); border-bottom: 1px solid #18191c;">
        <span class="navbar-brand mb-0 h1 text-white"><i class="fas fa-video mr-2"></i>CIRAS MEET</span>
        <div>
            <span class="text-white mr-3">Bonjour, <b><%= username %></b></span>
            <a href="/logout" class="btn btn-sm btn-outline-danger">Déconnexion</a>
        </div>
    </nav>

    <div class="auth-container">
        <div class="auth-card" style="max-width: 600px;">
            <div class="text-center mb-4">
                <h2>Prêt pour votre réunion ?</h2>
                <p class="text-secondary">Démarrez une nouvelle visioconférence ou rejoignez une salle existante.</p>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <form action="/create-room" method="POST">
                        <button type="submit" class="btn btn-primary-discord btn-block py-4">
                            <i class="fas fa-plus-circle fa-2x mb-2"></i><br>
                            Nouvelle Réunion
                        </button>
                    </form>
                </div>
                <div class="col-md-6">
                    <div class="btn btn-dark btn-block py-4" style="background-color: #2f3136; border: 1px solid #202225; cursor: default;">
                        <i class="fas fa-keyboard fa-2x mb-2 text-secondary"></i><br>
                        Rejoindre via Code
                    </div>
                </div>
            </div>

            <div class="or-divider">
                <span>OU REJOINDRE MANUELLEMENT</span>
            </div>

            <form id="joinForm" class="d-flex">
                <input type="text" id="roomInput" class="form-control form-control-dark mr-2" placeholder="Entrez le code (ex: 1234)...">
                <button type="submit" class="btn btn-secondary">Go</button>
            </form>
        </div>
    </div>

    <script>
        document.getElementById('joinForm').addEventListener('submit', (e) => {
            e.preventDefault();
            const roomCode = document.getElementById('roomInput').value;
            if(roomCode) {
                window.location.href = `/${roomCode}`;
            }
        });
    </script>
</body>
</html>